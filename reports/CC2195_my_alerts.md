# cc#2195 — My Alerts: ONE page (Triggered · Pending · Create), the Home bell and the My Alerts tile both open it, Create = the cc#2095 builder full-screen

Founder 17-Sep-2026 15:13 IST (voice + two screenshots). Built 17-Sep-2026 from 17:16 IST server time; the landing time is in the task row.

## Decision stated up front (reversible, one href)

The one page lives at **/m/myalerts** — the route the My Alerts grid tile already opens and the page in the founder's second screenshot ("Read from /api/alerts/list" is `mobile/myalerts.html`'s own as-of line). `/m/alerts` today is the **Trade Alerts ideas page** (cc#1620, bottom-nav slot 5 "Alerts", the EXPERT HANDPICKED approved book) — a different surface the card does not mention; it is untouched, and its `#new` SET ALERT path stays alive (Fable log 7055). If the page should sit at `/m/alerts` instead, that is one route + one href.

## What shipped

| File | Change |
|---|---|
| `mobile/myalerts.html` | REWRITTEN. Sticky tab bar under the header — **Triggered n · Pending n · Create** (chosen over stacked anchors: one section on screen at a time, hash-addressable `#triggered` `#pending` `#create`, and the legacy `#new` lands on Create with the builder open). TRIGGERED: every fired alert newest first, grouped by day (Today / Yesterday / date); card = TC Scanner grammar (left accent, "Fired 17 Sep · 10:05" eyebrow, tag CUSTOM · n conditions or BUY/SELL · price alert, big symbol → `/m/check?sym=`, "set …" stamp, condition chips with the last value, price line "crossed ≤ ₹400", then "Then: awaiting a decision / approved @ … / dismissed"). PENDING: the same card with the conditions as chips joined by AND / OR pills, "crosses ≥ ₹1,600 · live ₹1,543.2" for price alerts, a check-cadence line and a **⋯** button; ⋯ or a long-press (550 ms) opens a bottom sheet: **Edit — change the conditions** (custom only) · **Delete this alert** (tap twice) · Cancel. CREATE: a card with **＋ Create custom alert** (the cc#2095 builder) and **Set a simple price alert** (the cc#1831 form, so the popover's SET ALERT flow survives), the 5 metric categories with counts, and where alerts land. Opening the page marks the founder's unseen price alerts seen (`POST /api/alerts/seen`) and remembers the custom alerts it showed (localStorage), so the Home badge clears. Empty states are sentences, never blank. The "Read from /api/alerts/list" line, the "Custom alerts only, for now" explainer and the 0 / SET BY YOU / FIRED OR DISMISSED counter card are gone. Every class `al-` prefixed, tokens only. |
| `trade_alerts_app.py` | `GET /api/mobile/myalerts` REPLACED (its only reader was the old page). Sources unchanged and un-forked: `trade_alerts_endpoints.list_alerts()` (live cmp, seen flags), `custom_alerts.list_custom_alerts()` + `get_registry()`. Pure `shape()`: triggered = every custom alert with `triggered_at` + every founder-own price alert (`source_engine IS NULL`, kind entry/exit) with `triggered_at`, sorted by `triggered_at` desc (the bell's own key); pending = custom `active` + price `pending`, newest set first. Engine approvals and QB rebalance notices stay off (the bell's isolation rule). Condition text "Daily RSI below 50" from the registry labels (the bell's phrasing); IST stamps "15 Sep · 19:30"; counts (triggered, pending, triggered today, unseen price). |
| `scorr_bell.js` | On `/m/*` the bell is a **link to /m/myalerts** (`data-bell-link`), no popover; the web nav keeps its popover untouched. App badge = the server's unseen count for the founder's own price alerts (trade_alert_seen, as before) **+ custom alerts triggered today (IST) not yet opened on the page** (custom rows carry no server seen flag — cc#2095 v1 — so the page remembers them locally under `scorr_alerts_seen_custom`). `ALERTS` (a row's app landing) is `/m/myalerts`. No literal colours added (the file's pre-existing cc#1998 ratchet count is unchanged). |
| `scorr_custom_alert_create.js` | Additive, not a fork: (1) `MAX_CONDS = 5` — the add button counts "n of 5" and disables at the cap; (2) **full-screen at ≤480 px** (the app's Create Alert), the centred modal stays on the web; (3) `open(onCreated, prefill)` — `{symbol, label, conditions, replace_id}` opens the same form as **Edit custom alert / Save changes**; on save the new alert is created FIRST and only then is `replace_id` deleted (no update endpoint exists; the cc#2095 backend is untouched; a failed create can never lose the alert). |
| `scorr_custom_alert_create.js`, `scorr_alert_create.js` | **Defect found and fixed in-card (one attribute each):** `scorr_card_common.js`'s capture-phase `[data-sym]` tap handler on every `/m/` page was opening the symbol card sheet over both create forms when a search result was tapped, and its `stopPropagation` meant the result's own click never set the symbol — the builder's symbol search did not work on the app. Both overlays now carry `[data-scorr-skip]`, the handler's own opt-out (cc#911 precedent). Reproduced in the harness before the fix (the sheet intercepted the tap), gone after. |
| `main.py` | The `NAV_REGISTRY["/m/myalerts"]` description only (wiring unchanged: the route, PROTECTED and the My Scorr tile already existed). |
| `tests/test_myalerts_app.py` | 5 tests: IST stamp; condition and price text; triggered newest first with the day key and today flag; pending text/live text; V8 approval, QB notice, dismissed-never-fired and the bell's merged custom copy all excluded; registry flattening. 5 passed. |
| `reports/CC2195_my_alerts.md` | This report. |

## Production facts (17-Sep 17:20 IST server time, run_sql)

`custom_alerts`: 1 row — id 1 RELIANCE "Daily RSI below 50", triggered 2026-09-15 14:00:02 UTC = **15 Sep · 19:30 IST**, last value 33.22. `trade_alerts`: 12 rows — 7 V8 approvals, 4 QB rebalance notices (source_engine set → off the page), 1 founder-own price alert (id 24 RELIANCE BUY ≥ ₹1,500, dismissed, never triggered → not "fired"). So on production today the page shows **Triggered 1 (RELIANCE on top) · Pending 0**, and the bell badge is 0 once the page has been opened.

## Harness (Playwright, real Chromium, `scratchpad/cc2195_test.py`) — ALL PASS at 375×812, goldnight + aquawhite, plus 360×780

The payload is produced by the shipped `trade_alerts_app.shape()` over stateful mocks; the registry is the live `alert_metric_registry` (10 metrics, 5 categories). Two fixtures: **A** = the production rows above; **B** = A + TCS custom pending (2 conditions AND, last value 7.4), INFY price pending (live 1,543.2, unseen), ITC price triggered today 10:05 (unseen), HDFCBANK custom triggered today 09:20.

- **A:** tabs "Triggered 1 · Pending 0 · Create", Triggered selected; one card: RELIANCE, chip "Daily RSI below 50 · now 33.22 · 15 Sep · 19:30", "Fired 15 Sep · 19:30", tag Custom · 1 condition, under the day header "15 Sep 2026"; Pending shows "No alert is waiting …"; the tab bar's top equals the header height (sticky); no overflow; the three removed strings are absent from the source.
- **Home bell (B):** the bell carries `data-bell-link="/m/myalerts"`, badge **3** (2 unseen price alerts + 1 custom fired today); tap → `/m/myalerts`, no popover element; opening the page POSTs `/api/alerts/seen` with ids 30, 31 and stores c1/c3 locally; back on Home the badge is hidden. The My Alerts tile is present.
- **Triggered (B):** "Triggered 3 · Pending 2", sub "2 fired today · 2 waiting"; order ITC (10:05) → HDFCBANK (09:20) under **Today**, RELIANCE under **15 Sep 2026**; ITC: "crossed ≤ ₹400", SELL tag, red accent, "Then: awaiting a decision".
- **Pending (B):** `#pending` cold-opens the tab; TCS above INFY; TCS chips "Day Move % above 2.5%" AND "GVM Score above 7 · now 7.4 · 17 Sep · 15:30"; INFY "crosses ≥ ₹1,600 · live ₹1,543.2", BUY tag; ⋯ → sheet "TCS · custom alert" with Edit / Delete / Cancel.
- **Edit:** the builder opens **full-screen (375 px wide, x = 0)** titled "Edit custom alert", symbol TCS, label, both rows prefilled (Day Move % (%) above 2.5 · GVM Score (score) above 7, AND), button "Save changes"; change 7 → 7.5, save → `POST /api/custom_alerts/create` (TCS, threshold 7.5, AND) **then** `POST /api/custom_alerts/delete {id: 2}`; Pending shows the edited TCS on top with "GVM Score above 7.5", the old row gone.
- **Create:** the Create tab (`#create`) shows the two buttons and 5 categories; the builder opens full-screen with "+ add condition (1 of 5)"; four taps → 5 rows, a sixth does nothing and the button reads "5 of 5 conditions -- the most one alert can carry" (disabled); symbol search "REL" → tap RELIANCE (sets the symbol — the fixed defect); five metrics picked through the categorised picker with thresholds 45 / 2 / 7 / 5 / 1.5, row 1 "below", row 2 joined by OR; save → the POST body is RELIANCE with 5 conditions and joins [null, OR, AND, AND, AND]; the page lands on Pending with the new alert on top (5 chips, OR/AND pills), tab count 3.
- **Delete:** long-press INFY → sheet (price alert: Delete only); Delete → "Tap again to delete INFY" → `POST /api/alerts/dismiss {id: 30}`; INFY gone, tab count 2.
- `#new` → Create tab with the builder open. 360 px: no overflow on either list. No page errors on either theme.

**Screenshots looked at** (`scratchpad/cc2195_*.png`): `375_dark_prod_triggered` — the production replicate: "MY ALERTS · 0 waiting", Triggered 1 pressed, "15 SEP 2026", the RELIANCE card with the gold Custom tag, the chip "Daily RSI below 50 / now 33.22 · 15 Sep · 19:30" and the label line. `375_dark_triggered` — "2 fired today · 2 waiting", TODAY with ITC (red accent, SELL · PRICE ALERT, crossed ≤ ₹400, "Then: awaiting a decision") and HDFCBANK, then 15 SEP 2026 with RELIANCE. `375_light_pending` — Pending 2 pressed on aquawhite: TCS with the two chips and the AND pill, the cadence line and the ⋯ button; INFY with "crosses ≥ ₹1,600 · live ₹1,543.2". `375_light_sheet` — the bottom sheet "TCS · CUSTOM ALERT" with Edit / Delete (red) / Cancel over the dimmed list. `375_dark_builder5` — the builder full-screen: Condition 2 … 5 rows with AND/OR pills, "5 of 5 conditions -- the most one alert can carry", Create custom alert, Cancel.

## Live checks (Fable — the sandbox cannot reach scorr.in)

1. `https://scorr.in/api/mobile/myalerts` → `triggered[0]` = RELIANCE, text "Daily RSI below 50", `fired_ist` "15 Sep · 19:30"; `pending` = []; `counts.triggered` 1.
2. `https://scorr.in/m/home` → the bell is a link (no popover); tap → `/m/myalerts`; the My Alerts tile → the same page. `https://scorr.in/m/myalerts` → RELIANCE on top of Triggered; Create → the builder full-screen, 5-row cap; a search result tap sets the symbol (the fixed defect); Save → Pending.
3. `https://scorr.in/alerts` (web) → the bell popover still opens (unchanged).

## Out of scope

The alert engine / evaluation and the cc#2095 backend (untouched). `/m/alerts` (Trade Alerts ideas) untouched. A server-side seen flag for custom alerts (v1 keeps it local) — a small backend card if wanted.
